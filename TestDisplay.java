import org.junit.*;
import static org.junit.Assert.*;

public class TestDisplay{
  @Test public void testdrawmenu(){
    Display display = new DisplayImpl();
    assertTrue(display.ReDrawGameBoard(log[], gameboard[][]);
    assertTrue(display.DrawMenu());
  }
}
